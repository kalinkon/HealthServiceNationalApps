<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mcc.healthservicefinal">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainMenuActivity" android:theme="@style/MenuTheme">
        </activity>
        <activity android:name=".activity.HospitalListActivity"
            android:theme="@style/SubMenuDiseaseLisTheme">

        </activity>

        <activity android:name=".activity.SubMenuDiseaseListActivity"
            android:theme="@style/SubMenuDiseaseLisTheme">

        </activity>
        <activity android:name=".activity.ContentDiseaseActivity"
            android:theme="@style/AppThemeDiseaseContent"></activity>
    </application>

</manifest>